{
  "name": "inbox-only",
  "version": "2.0.0",
  "description": "Browser extension that locks social sites to their inbox.",
  "private": true,
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test \"test/*.test.js\"",
    "test:e2e": "node --test \"test/e2e/*.test.js\""
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  },
  "license": "MIT"
}
